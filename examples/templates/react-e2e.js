import React from 'react';
import {shallow} from 'enzyme';
import {pl8.name} from './{pl8.name}';

it('', () => {
  // Render component
  const component = shallow(
    <{pl8.name} />
  );

  expect().to();
});
